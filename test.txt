program prueba:

var int global1, global2;

function void fibonacci(int a){
    var int nousamos;
    if ( a == 1 ){
        print(0);
    }
    nousamos = 0;

    if ( a > 1 ){
        print(1);
    }

    nousamos = 0;

    if ( a == 2 ){
        print(1);
    }

    nousamos = 0;
    
    if ( a > 2 ){
        print(1);
        imprimir(a-1, 1, 1);
    }
}

function void fibonacciParte2(int a, int b, int c){
    var int x, aux;

    if( a < 1 ){
        x = 0;
    } else {
        x= b + c;
        print(x);
        aux = b;
        b = c;
        c = aux + c;
        fibonacciParte2(a-1, b, c);
    }
}

main(){
    var float por, por2;
    int x;
    input(x);
    fibonacci(x);
}